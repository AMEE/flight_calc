indexer
{
}

searchd
{
  address = 127.0.0.1
  port = 3312
  log = /opt/apps/cnn-carbon-calculator/releases/20090604165424/log/searchd.log
  query_log = /opt/apps/cnn-carbon-calculator/releases/20090604165424/log/searchd.query.log
  pid_file = /opt/apps/cnn-carbon-calculator/releases/20090604165424/log/searchd.production.pid
}

source airport_core_0
{
  type = mysql
  sql_host = localhost
  sql_user = root
  sql_pass = 
  sql_db = cnn_carbon_production
  sql_query_pre = SET NAMES utf8
  sql_query = SELECT `airports`.`id` * 1 + 0 AS `id` , CAST(`airports`.`country` AS CHAR) AS `country`, CAST(`airports`.`name` AS CHAR) AS `name`, CAST(`airports`.`full_name` AS CHAR) AS `full_name`, CAST(`airports`.`iata_code` AS CHAR
) AS `iata_code`, `airports`.`id` AS `sphinx_internal_id`, 2972503646 AS `class_crc`, '2972503646' AS `subclass_crcs`, 0 AS `sphinx_deleted` FROM `airports`    WHERE `airports`.`id` >= $start AND `airports`.`id` <= $end GROUP BY `airpo
rts`.`id`  ORDER BY NULL
  sql_query_range = SELECT IFNULL(MIN(`id`), 1), IFNULL(MAX(`id`), 1) FROM `airports` 
  sql_attr_uint = sphinx_internal_id
  sql_attr_uint = class_crc
  sql_attr_uint = sphinx_deleted
  sql_attr_multi = uint subclass_crcs from field
  sql_query_info = SELECT * FROM `airports` WHERE `id` = (($id - 0) / 1)
}

index airport_core
{
  source = airport_core_0
  path = /opt/apps/cnn-carbon-calculator/releases/20090604165424/db/sphinx/production/airport_core
  morphology = stem_en
  charset_type = utf-8
  min_prefix_len = 3
  enable_star = 1
}

index airport
{
  type = distributed
  local = airport_core
}
